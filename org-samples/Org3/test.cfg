
Hello test file

ca:
  L: ${HLF_CA_L}
  C: ${HLF_CA_C}
  ST: ${HLF_CA_ST}
  O: ${HLF_CA_O}
  OU: ${HLF_CA_OU}
  hosts:
    ${HLF_CA_HOSTS}|awk -F, '{print $1}'
  for i in $(seq 10); do
    echo $i
  done
  cat h.cfg
